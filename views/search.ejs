<!doctype html>
<html>
  <head>
    <title>Search <%= shopData.shopName %></title>
    <link rel="stylesheet" type="text/css" href="/main.css" />
  </head>
  <body>
    <h1>Search <%= shopData.shopName %></h1>

    <form action="/books/search-result" method="GET">
      <p>What book do you want?</p>
      <input id="search-box" type="text" name="search_text" required>
      <input type="submit" value="Search">
    </form>

    <% if (typeof keyword !== 'undefined') { %>
      <h2>Results for: "<%= keyword %>"</h2>
      <% if (books.length > 0) { %>
        <ul>
          <% books.forEach(book => { %>
            <li><%= book.name %>, £<%= Number(book.price).toFixed(2) %></li>
          <% }) %>
        </ul>
      <% } else { %>
        <p>No books found.</p>
      <% } %>
    <% } %>
  </body>
</html>